export interface SimInterface {
    _id?: string,
    number?: string,
    provider?: string,
    start_at?: Date,
    time_limit?: number,
    starting_price?: number,
    last_price?: number,
    created_at?: Date
}